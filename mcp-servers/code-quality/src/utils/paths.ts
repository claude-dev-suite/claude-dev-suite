// SPDX-License-Identifier: MIT
/**
 * Path validation for the analyzers.
 *
 * Re-exported from `@dev-suite/shared` so the five analyzers share one
 * definition instead of five byte-identical copies of it.
 */

export { validateFilePath } from '@dev-suite/shared';
