# {{PROJECT_NAME}}

> Auto-generated by dev-suite init-project

## Project Configuration

- **Type**: {{PROJECT_TYPE}}
- **Frontend**: {{FRONTEND_FRAMEWORK}}{{FRONTEND_META}}
- **Backend**: {{BACKEND_FRAMEWORK}}
- **Database**: {{DATABASE_ORM}}
- **Testing**: {{TESTING_UNIT}} / {{TESTING_E2E}}
{{MONOREPO_SECTION}}
## Dev-Suite Integration

This project uses dev-suite MCP servers and agents. Configuration is stored in `.dev-suite.json`.

### MCP Servers Available

- `documentation` - Fetch docs for {{SUPPORTED_TECHS}}
- `database-query` - Execute safe SQL queries
- `docker-manager` - Manage Docker containers
- `api-tester` - Test REST APIs

## Agent Routing

When working on this project, use the appropriate specialized agents:

{{AGENT_ROUTING_SECTION}}

## Workflow

For feature implementation, follow this workflow:

1. **Planning** - Use `@architect` for design decisions
2. **Implementation** - Use stack-specific experts ({{IMPLEMENTATION_AGENTS}})
3. **Testing** - Use `@{{TESTING_AGENT}}` for unit tests, `@playwright-expert` for E2E
4. **Review** - Use `@code-reviewer` for quality checks
5. **Security** - Use `@security-auditor` for security review

## Skills Available

The following skills are loaded for this project's stack:

{{SKILLS_LIST}}

## Quick Commands

```bash
# Fetch documentation
/docs {{PRIMARY_TECH}}

# Show current configuration
/show-config

# Reconfigure project
/reconfigure

# Generate code
/generate component
```

## Documentation Access

Use the MCP documentation server to fetch docs:

```
fetch_docs({ technology: "{{PRIMARY_TECH}}", topic: "basics", source: "local" })
search_docs({ query: "your search term" })
list_topics({ technology: "{{PRIMARY_TECH}}" })
```

## Project-Specific Notes

{{PROJECT_NOTES}}
